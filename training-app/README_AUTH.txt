
# XPLORE Auth Setup with Supabase + Brevo SMTP

## ✅ Steps Completed
- Connected to Supabase with secure anon API key
- Enabled SMTP email using Brevo
- Supports: Signup, Login, Forgot Password, Email Confirm

## 📂 Files Included
- `auth.js` – handles all Supabase auth actions

## 🔧 Setup
1. Add `auth.js` to your project (e.g., `/training-app/`)
2. Use `signUp(email, password)`, `signIn(email, password)`, and `resetPassword(email)` as needed
3. Ensure email templates are enabled in Supabase SMTP config
