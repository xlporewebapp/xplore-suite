document.addEventListener('DOMContentLoaded', () => {
  const referralList = document.getElementById('referralList');
  referralList.innerHTML = '<li>No referrals found. Supabase integration coming soon.</li>';
});
